[Unit]
Description=Prometheus
After=network.target

[Service]
User=prometheus
Group=prometheus
Type=simple
ExecStart=/etc/prometheus/prometheus-2*/prometheus --config.file=/etc/prometheus/prometheus-2*/prometheusconf.yml
Restart=on-failure

[Install]
WantedBy=multi-user.target
